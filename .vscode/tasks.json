{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
			"type" : "shell",
			"command": "cargo build --features flutter --target aarch64-apple-ios --lib",
			"options": {
				"cwd": "${workspaceFolder}",
			},
		},
	]
}